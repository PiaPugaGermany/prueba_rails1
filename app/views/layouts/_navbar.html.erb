<!-- Nav Bar -->
<nav class="navbar navbar-expand-lg navbar navbar-dark bg-primary fixed-top ">
  <a class="navbar-brand" href="<%= todos_path %>">ToDo App</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="<%= todos_path %>">INDEX <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="<%= list_todo_path %>">LISTA</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="<%= todos_new_path %>">CREAR</a>
      </li>
    </ul>
  </div>
</nav>
<!-- Nav Bar -->
